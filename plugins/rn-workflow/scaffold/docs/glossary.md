# Glossary

Domain and project terms. Add a row when a new term appears that a newcomer wouldn't infer from the code.

| Term | Meaning |
| ---- | ------- |
| tech-DNA | The canonical code patterns in `docs/tech-dna.md` — every feature/fix copies these. |
| mapper (firewall) | The pure function that transforms a validated wire shape into a view-model; the only place wire→view-model changes live. |
| view-model | The app-facing shape a component consumes, decoupled from the API wire shape. |
| <FILL IN> | <your first domain term> |
