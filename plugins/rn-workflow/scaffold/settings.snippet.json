{
  "//": "Merged into the project's .claude/settings.json by /init-dna. These are PROJECT-level permissions (the workflow hooks themselves come from the rn-workflow plugin, so they are NOT listed here). Deny destructive git; ask before any .env edit.",
  "permissions": {
    "ask": ["Edit(**/.env*)", "Write(**/.env*)"],
    "deny": [
      "Bash(git push --force:*)",
      "Bash(git push -f:*)",
      "Bash(git push --force-with-lease:*)",
      "Bash(git reset --hard:*)",
      "Bash(git clean -f:*)"
    ]
  }
}
